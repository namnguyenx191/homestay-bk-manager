{
  "name": "homestay-booking-manager",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "mongo:up": "docker compose up -d",
    "start": "docker compose up -d && npm run dev",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "pm2": "^7.0.1"
  },
  "dependencies": {
    "imapflow": "^1.3.3",
    "mailparser": "^3.9.8"
  }
}
